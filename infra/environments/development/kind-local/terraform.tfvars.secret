# GitHub token - read from GITHUB_TOKEN environment variable
# Usage: export GITHUB_TOKEN="ghp_xxxxx" && terraform apply -var-file=terraform.tfvars.secret
github_token = var.github_token != "" ? var.github_token : ""
