apiVersion: v1
kind: Service
metadata:
  name: ros2-talker
  labels:
    app: ros2-talker
spec:
  selector:
    app: ros2-talker
  ports:
  - port: 7400
    targetPort: 7400
    name: dds
  clusterIP: None
