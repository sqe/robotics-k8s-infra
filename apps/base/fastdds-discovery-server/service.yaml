apiVersion: v1
kind: Service
metadata:
  name: fastdds-discovery-server
  labels:
    app: fastdds-discovery-server
spec:
  selector:
    app: fastdds-discovery-server
  ports:
  - port: 11811
    targetPort: 11811
    name: discovery
  clusterIP: None
